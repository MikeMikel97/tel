# 🔐 Настройка HTTPS для calls4ai.ru

## 📋 Что нужно сделать:

### 1️⃣ Настроить DNS (В панели управления доменом)

Зайди туда, где покупал домен (Reg.ru, Beget, и т.д.) и добавь **A-записи**:

```
Запись 1:
Тип: A
Имя: @ (или оставь пустым, или напиши calls4ai.ru)
Значение: 46.254.18.120
TTL: 300 (или Auto/Default)

Запись 2:
Тип: A  
Имя: www
Значение: 46.254.18.120
TTL: 300
```

**Проверка DNS (через 5-10 минут после настройки):**
```bash
ping calls4ai.ru
# Должен показать IP: 46.254.18.120
```

---

### 2️⃣ Запустить скрипт на сервере

После того как DNS настроен и работает:

```bash
# Подключаемся к серверу
ssh root@46.254.18.120

# Переходим в директорию проекта
cd /opt/telephony

# Делаем скрипт исполняемым
chmod +x init-ssl.sh

# Запускаем настройку SSL (бесплатный Let's Encrypt)
./init-ssl.sh
```

Скрипт автоматически:
- ✅ Проверит DNS
- ✅ Получит бесплатный SSL сертификат от Let's Encrypt
- ✅ Настроит автоматическое обновление (каждые 12 часов)
- ✅ Перезапустит Nginx с HTTPS

---

## 🎯 Результат:

После успешной настройки сайт будет доступен по адресу:

- 🌐 **Frontend (Operator UI):** https://calls4ai.ru
- 🔧 **Admin Panel:** https://calls4ai.ru/admin
- 📚 **API Docs:** https://calls4ai.ru/docs
- 🔄 **HTTP автоматически перенаправляется на HTTPS**

---

## 🔒 Важно:

- **SSL сертификат БЕСПЛАТНЫЙ** (Let's Encrypt) - НЕ покупай за 2000р!
- Сертификат **обновляется автоматически** каждые 90 дней
- **IP адрес НЕ меняется** - остается 46.254.18.120
- Asterisk WebSocket автоматически работает через `wss://` (защищенный WebSocket)

---

## ❓ Проблемы?

### DNS не резолвится
- Подожди 5-30 минут (DNS обновление)
- Проверь: `dig calls4ai.ru` или `nslookup calls4ai.ru`

### Ошибка получения сертификата
- Убедись что DNS настроен правильно
- Проверь что порты 80 и 443 открыты: `sudo ufw status`
- Открой порты если нужно: `sudo ufw allow 80/tcp && sudo ufw allow 443/tcp`

---

## 🚀 После настройки:

Обнови ссылки в UI - вместо `http://46.254.18.120:3003` используй `https://calls4ai.ru`
