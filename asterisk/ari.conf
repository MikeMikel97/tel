; ============================================
; ARI (Asterisk REST Interface) Configuration
; Скопировать в /etc/asterisk/ari.conf
; ============================================

[general]
enabled=yes
pretty=yes
allowed_origins=*

[ai-agent]
type=user
read_only=no
password=aiagent_secret_password_2024
password_format=plain
